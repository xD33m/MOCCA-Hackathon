import { FadeInImageDirective } from './fade-in-image.directive';

describe('FadeInImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInImageDirective();
    expect(directive).toBeTruthy();
  });
});
